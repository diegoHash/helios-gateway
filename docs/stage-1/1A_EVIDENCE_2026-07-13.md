# Evidencia de Etapa 1A — 2026-07-13

Estado: implementación de código completada; puerta operacional abierta.

## Cambios aplicados

### CAC

- `.env.prod` retirado del árbol versionado y reemplazado por `.env.example` sin valores.
- `build/` retirado del seguimiento para evitar persistir configuraciones compiladas y artefactos.
- `docker-compose.yml` consume variables externas y exige DB/JWT; ya no contiene credenciales ni host productivo embebidos.
- Token, chat y nombre de Telegram se inyectan; la integración omite envíos cuando no está configurada.
- El controlador usa el bean configurado y no construye una instancia con credenciales propias.
- Seeder conocido reemplazado por bootstrap explícito, deshabilitado por defecto y con parámetros externos.
- JWT, contraseña DB y clave de cifrado ya no tienen defaults utilizables.

### CBT

- El script de consulta exige conexión DB mediante variables de entorno.
- El token de WhatsApp ya no se escribe en logs; la integración queda inactiva sin token/phone ID.
- Se añadió una plantilla `.env.example` sin valores y se mantuvieron ignorados los `.env` reales.
- JWT, contraseña DB y clave de cifrado ya no tienen defaults utilizables.
- Telegram, Gemini, OpenWeather y WhatsApp usan valores vacíos cuando son opcionales, nunca credenciales ficticias compartidas.

### Super Admin

- `CBT_AES_COMPAT_KEY` ya no tiene una clave global por defecto y debe contener exactamente 32 caracteres.
- La plantilla no incluye el valor anteriormente compartido.

### Coordinación

- Plan maestro y runbook de rotación versionados en `helios-gateway/docs`.

## Verificaciones ejecutadas

| Repositorio | Verificación | Resultado |
|---|---|---|
| `cac_api` | `gradlew compileJava --no-daemon` | aprobada |
| `cac_api` | `gradlew test --no-daemon` | aprobada |
| `cbt_api` | `gradlew compileJava --no-daemon` | aprobada |
| `cbt_api` | `gradlew compileTestJava --no-daemon` | aprobada después de reparar una prueba de diagnóstico incompleta y retirar una prueba obsoleta de una clase inexistente |
| `cbt_api` | pruebas unitarias de controller/service/security sin `@SpringBootTest` | aprobadas |
| `cbt_api` | suite completa con dos `@SpringBootTest` | agotó 180 segundos al intentar levantar dependencias externas; requiere ambiente de integración |
| Super Admin API | `npm run typecheck` y `npm run build` | aprobadas |
| Super Admin API | `npm run lint` | aprobada después de incorporar flat config para ESLint 9 |
| `helios-gateway` | `gradlew test --no-daemon` | aprobada; el proyecto incorporó posteriormente una prueba de arranque mínima |
| cuatro repositorios | búsqueda de cadenas comprometidas/defaults conocidos en archivos seguidos | sin coincidencias |

`gitleaks` no está instalado en el entorno; su ejecución o un escáner equivalente continúa siendo obligatoria para la puerta 1A.

## Pendientes que impiden cerrar 1A

1. Rotar y revocar en proveedores todas las credenciales enumeradas en `SECURITY_ROTATION_RUNBOOK.md`.
2. Aprovisionar las nuevas variables mediante el gestor de secretos en cada ambiente y ejecutar transacciones sintéticas.
3. Ejecutar un escáner de secretos sobre árbol, historial, artefactos e imágenes.
4. Proveer un ambiente/perfil de integración aislado para ejecutar los dos `@SpringBootTest` de CBT sin depender de infraestructura productiva.
5. Decidir y autorizar, después de las rotaciones, si se reescribirá el historial Git.

No se inicia 1B en un ambiente desplegado hasta revocar las credenciales anteriores. El desarrollo local de 1B puede prepararse en commits separados, pero no debe mezclarse con este hito.
